{
  "name": "inked",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "db:init": "node scripts/init-db.js"
  },
  "description": "Inked — React frontend with Express API, PostgreSQL, and auth",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-session": "^1.19.0",
    "pg": "^8.22.0"
  }
}
